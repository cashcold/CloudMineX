# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (Optional - Defaults to built-in JSON file storage if not provided)
MONGODB_URI=

# Authentication & Email
JWT_SECRET=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SENDER_EMAIL=

# Payment & Mobile Money Gateways (Optional for live production integration)
HUBTEL_CLIENT_ID=
HUBTEL_CLIENT_SECRET=
PAYSTACK_SECRET_KEY=
NOWPAYMENTS_API_KEY=

# Admin Configuration
ADMIN_DEFAULT_KEY=cloudminex_secure_admin_2026
