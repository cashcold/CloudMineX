# MongoDB Connection String (Optional - falls back to persistent in-memory/JSON store if omitted)
MONGO_URI=

# Port configuration (Defaults to 3000)
PORT=3000

# Client API Base URL (Optional - defaults to /api)
VITE_API_URL=
